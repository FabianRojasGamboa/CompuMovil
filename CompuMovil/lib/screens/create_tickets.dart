import 'package:flutter/material.dart';

class CreateTickets extends StatelessWidget {
  const CreateTickets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Formulario'));
  }
}
